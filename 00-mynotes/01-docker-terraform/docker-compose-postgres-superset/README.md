
# Commands

Windows

# Build docker compose file
```
docker compose build
```

# Start the container

```
docker compose up
```
# Start the container and loading part in terminal

```
docker compose up -d
```
# Stop the container

```
docker compose down
```
